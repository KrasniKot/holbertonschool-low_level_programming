#include "main.h"

/**
 * _abs - return absolute value of a.
 * @a: _abs variable.
 * Return: absolute value of a.
 */
int _abs(int a)
{

	if (a <= 0)
	{
		return (-a);
	}
	else
	{
		return (a);
	}
}
