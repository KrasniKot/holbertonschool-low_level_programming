#include "main.h"

/**
 * puts2 - prints every other character of a string.
 * @str: function variable.
 */

void puts2(char *str)
{
	int i = 0, k = 0;

	while (str[k])
	{
		k++
	}

	for (i = 0; i < k - 1; i++)
	{
		_putchar (k[i]);
		i++;
	}
	_putchar (10);
}

