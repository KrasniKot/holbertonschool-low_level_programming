#include "main.h"

/**
 * _abs - return absolute value of a.
 * @a: _abs variable.
 * Return: absolute value of a.
 */
int _abs(int a)
{
	int b = -2;
	int c = 2;

	if (a < 0)
	{
		return ((a * b) / c);
	}
	else
	{
		return (a);
}
