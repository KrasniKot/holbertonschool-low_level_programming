#include "main.h"

int comparing(char *s1, char *s2, int i)
{
	if (s1[i] != s2[i])
	{
		if (s1[i] == '*' || s2[i] == '*')
		{
		}
		return (0)
	}
	else if (
}
int wildcmp(char *s1, char *s2)
{
	return (comparing(s1, s2, 0));
}
